1.Docker & Jenkins & CI
项目自动化部署

2.前端项目搭建
webpack分环境配置

3.node.js
process.enx.NODE_ENV
主要用途是：在使用 node.js 环境执行 JavaScript 脚本时，通过这个属性来区分不同环境（开发、生产、测试等）下的处理（构建、运行等）策略。

除了 VUE_APP_* 变量之外，在你的应用代码中始终可用的还有两个特殊的变量：
--也就是说：除了 baseUrl 和 NODE_ENV 其他环境变量使用 VUE_APP开头
--NODE_ENV - 会是 "development"、"production" 或 "test" 中的一个。具体的值取决于应用运行的模式。
--BASE_URL - 会和 vue.config.js 中的 publicPath 选项相符，即你的应用会部署到的基础路径。